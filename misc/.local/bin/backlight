#!/bin/env /bin/bash
[[ ! "$@" ]] && exit 1

min=1
step=20

[[ "$1" == "-dec" && $(($(xbacklight -get | sed 's/\.[0-9]\+//') - $step)) -le 0 ]] && \
  xbacklight -set $min || \
  xbacklight $1 $step

unset min
unset step
