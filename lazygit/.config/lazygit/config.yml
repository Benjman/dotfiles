os:
  edit: 'nvim --cmd "let g:block_session=1" {{filename}}'
  editPreset: 'nvim --cmd "let g:block_session=1" {{filename}}'

gui:
  theme:
    selectedLineBgColor:
      - underline
    selectedRangeBgColor:
      - underline

git:
  autoRefresh: false
  paging:
    colorArg: never
    pager: delta --dark --paging=never
